import React from 'react';
import {hot} from 'react-hot-loader/root';
import '../scss/app.scss';

const App = () => (
  <div className="app">
    <h1>Webpack + Babel + Eslint + Prettier + Sass + React!!!</h1>
  </div>
);

export default hot(App);
